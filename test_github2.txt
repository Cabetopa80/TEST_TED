def management_view(df, f):
    fdf = df[f]
    #fdf['Approved by SCAG'] = df['Comments and Work def tes@ted.com'].apply(find_approval_str)
    fdf['Approved by SCAG'] = ['Not known']*len(fdf)
    #fdf['SIP/BAU'] = ['Not known']*len(fdf)